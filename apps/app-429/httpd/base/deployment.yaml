apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-429
  labels:
    app: app-429
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-429
  template:
    metadata:
      labels:
        app: app-429
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
