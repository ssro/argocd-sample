apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-63
  labels:
    app: app-63
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-63
  template:
    metadata:
      labels:
        app: app-63
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
