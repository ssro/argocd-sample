apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-601
  labels:
    app: app-601
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-601
  template:
    metadata:
      labels:
        app: app-601
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
