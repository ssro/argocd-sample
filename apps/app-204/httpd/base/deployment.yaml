apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-204
  labels:
    app: app-204
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-204
  template:
    metadata:
      labels:
        app: app-204
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
