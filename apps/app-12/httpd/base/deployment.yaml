apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-12
  labels:
    app: app-12
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-12
  template:
    metadata:
      labels:
        app: app-12
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
