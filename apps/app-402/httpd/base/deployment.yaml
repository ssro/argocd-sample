apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-402
  labels:
    app: app-402
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-402
  template:
    metadata:
      labels:
        app: app-402
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
