apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-455
  labels:
    app: app-455
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-455
  template:
    metadata:
      labels:
        app: app-455
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
