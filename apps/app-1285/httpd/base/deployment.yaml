apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1285
  labels:
    app: app-1285
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1285
  template:
    metadata:
      labels:
        app: app-1285
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
