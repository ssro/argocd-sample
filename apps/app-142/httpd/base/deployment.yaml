apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-142
  labels:
    app: app-142
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-142
  template:
    metadata:
      labels:
        app: app-142
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
