apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-882
  labels:
    app: app-882
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-882
  template:
    metadata:
      labels:
        app: app-882
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
