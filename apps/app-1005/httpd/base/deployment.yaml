apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1005
  labels:
    app: app-1005
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1005
  template:
    metadata:
      labels:
        app: app-1005
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
