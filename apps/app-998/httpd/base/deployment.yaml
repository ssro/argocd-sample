apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-998
  labels:
    app: app-998
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-998
  template:
    metadata:
      labels:
        app: app-998
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
