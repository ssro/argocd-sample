apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-705
  labels:
    app: app-705
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-705
  template:
    metadata:
      labels:
        app: app-705
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
