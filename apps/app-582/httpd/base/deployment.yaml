apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-582
  labels:
    app: app-582
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-582
  template:
    metadata:
      labels:
        app: app-582
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
