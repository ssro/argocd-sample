apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-914
  labels:
    app: app-914
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-914
  template:
    metadata:
      labels:
        app: app-914
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
