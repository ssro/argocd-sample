apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1001
  labels:
    app: app-1001
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1001
  template:
    metadata:
      labels:
        app: app-1001
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
