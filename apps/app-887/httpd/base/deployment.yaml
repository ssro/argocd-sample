apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-887
  labels:
    app: app-887
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-887
  template:
    metadata:
      labels:
        app: app-887
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
