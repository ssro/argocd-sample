apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-470
  labels:
    app: app-470
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-470
  template:
    metadata:
      labels:
        app: app-470
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
