apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-186
  labels:
    app: app-186
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-186
  template:
    metadata:
      labels:
        app: app-186
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
