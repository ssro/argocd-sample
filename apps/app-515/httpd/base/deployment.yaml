apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-515
  labels:
    app: app-515
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-515
  template:
    metadata:
      labels:
        app: app-515
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
