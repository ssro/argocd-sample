apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-327
  labels:
    app: app-327
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-327
  template:
    metadata:
      labels:
        app: app-327
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
