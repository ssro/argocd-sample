apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-40
  labels:
    app: app-40
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-40
  template:
    metadata:
      labels:
        app: app-40
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
