apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-148
  labels:
    app: app-148
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-148
  template:
    metadata:
      labels:
        app: app-148
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
