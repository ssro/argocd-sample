apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-294
  labels:
    app: app-294
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-294
  template:
    metadata:
      labels:
        app: app-294
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
