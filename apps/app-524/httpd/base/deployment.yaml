apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-524
  labels:
    app: app-524
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-524
  template:
    metadata:
      labels:
        app: app-524
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
