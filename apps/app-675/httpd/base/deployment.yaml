apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-675
  labels:
    app: app-675
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-675
  template:
    metadata:
      labels:
        app: app-675
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
