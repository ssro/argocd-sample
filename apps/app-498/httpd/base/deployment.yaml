apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-498
  labels:
    app: app-498
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-498
  template:
    metadata:
      labels:
        app: app-498
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
