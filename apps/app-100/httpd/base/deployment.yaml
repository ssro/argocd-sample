apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-100
  labels:
    app: app-100
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-100
  template:
    metadata:
      labels:
        app: app-100
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
