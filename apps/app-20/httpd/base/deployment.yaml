apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-20
  labels:
    app: app-20
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-20
  template:
    metadata:
      labels:
        app: app-20
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
