apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-899
  labels:
    app: app-899
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-899
  template:
    metadata:
      labels:
        app: app-899
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
