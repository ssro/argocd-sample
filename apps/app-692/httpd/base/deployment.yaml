apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-692
  labels:
    app: app-692
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-692
  template:
    metadata:
      labels:
        app: app-692
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
