apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-955
  labels:
    app: app-955
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-955
  template:
    metadata:
      labels:
        app: app-955
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
