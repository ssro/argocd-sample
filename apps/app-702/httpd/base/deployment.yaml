apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-702
  labels:
    app: app-702
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-702
  template:
    metadata:
      labels:
        app: app-702
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
