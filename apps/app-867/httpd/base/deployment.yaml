apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-867
  labels:
    app: app-867
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-867
  template:
    metadata:
      labels:
        app: app-867
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
