apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-158
  labels:
    app: app-158
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-158
  template:
    metadata:
      labels:
        app: app-158
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
