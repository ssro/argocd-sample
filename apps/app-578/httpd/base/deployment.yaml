apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-578
  labels:
    app: app-578
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-578
  template:
    metadata:
      labels:
        app: app-578
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
