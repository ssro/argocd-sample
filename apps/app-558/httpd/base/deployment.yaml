apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-558
  labels:
    app: app-558
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-558
  template:
    metadata:
      labels:
        app: app-558
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
