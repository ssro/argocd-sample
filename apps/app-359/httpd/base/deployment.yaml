apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-359
  labels:
    app: app-359
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-359
  template:
    metadata:
      labels:
        app: app-359
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
