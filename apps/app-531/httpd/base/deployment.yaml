apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-531
  labels:
    app: app-531
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-531
  template:
    metadata:
      labels:
        app: app-531
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
