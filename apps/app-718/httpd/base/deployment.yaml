apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-718
  labels:
    app: app-718
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-718
  template:
    metadata:
      labels:
        app: app-718
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
