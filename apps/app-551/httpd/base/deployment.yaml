apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-551
  labels:
    app: app-551
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-551
  template:
    metadata:
      labels:
        app: app-551
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
