apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-850
  labels:
    app: app-850
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-850
  template:
    metadata:
      labels:
        app: app-850
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
