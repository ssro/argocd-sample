apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-66
  labels:
    app: app-66
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-66
  template:
    metadata:
      labels:
        app: app-66
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
