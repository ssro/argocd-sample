apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-393
  labels:
    app: app-393
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-393
  template:
    metadata:
      labels:
        app: app-393
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
