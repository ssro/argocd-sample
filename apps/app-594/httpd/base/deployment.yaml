apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-594
  labels:
    app: app-594
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-594
  template:
    metadata:
      labels:
        app: app-594
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
