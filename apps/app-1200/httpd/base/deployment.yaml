apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1200
  labels:
    app: app-1200
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1200
  template:
    metadata:
      labels:
        app: app-1200
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
