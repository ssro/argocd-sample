apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-55
  labels:
    app: app-55
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-55
  template:
    metadata:
      labels:
        app: app-55
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
