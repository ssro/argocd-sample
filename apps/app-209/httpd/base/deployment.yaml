apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-209
  labels:
    app: app-209
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-209
  template:
    metadata:
      labels:
        app: app-209
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
