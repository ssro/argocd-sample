apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-172
  labels:
    app: app-172
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-172
  template:
    metadata:
      labels:
        app: app-172
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
