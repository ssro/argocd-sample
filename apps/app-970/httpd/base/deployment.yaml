apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-970
  labels:
    app: app-970
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-970
  template:
    metadata:
      labels:
        app: app-970
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
