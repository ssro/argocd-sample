apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-101
  labels:
    app: app-101
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-101
  template:
    metadata:
      labels:
        app: app-101
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
