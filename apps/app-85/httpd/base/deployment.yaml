apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-85
  labels:
    app: app-85
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-85
  template:
    metadata:
      labels:
        app: app-85
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
