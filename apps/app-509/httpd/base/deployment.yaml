apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-509
  labels:
    app: app-509
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-509
  template:
    metadata:
      labels:
        app: app-509
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
