apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1280
  labels:
    app: app-1280
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1280
  template:
    metadata:
      labels:
        app: app-1280
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
