apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-32
  labels:
    app: app-32
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-32
  template:
    metadata:
      labels:
        app: app-32
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
