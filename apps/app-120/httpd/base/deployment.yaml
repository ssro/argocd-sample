apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-120
  labels:
    app: app-120
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-120
  template:
    metadata:
      labels:
        app: app-120
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
