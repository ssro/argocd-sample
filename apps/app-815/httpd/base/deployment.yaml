apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-815
  labels:
    app: app-815
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-815
  template:
    metadata:
      labels:
        app: app-815
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
