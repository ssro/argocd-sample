apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-80
  labels:
    app: app-80
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-80
  template:
    metadata:
      labels:
        app: app-80
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
