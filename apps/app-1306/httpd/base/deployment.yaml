apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1306
  labels:
    app: app-1306
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1306
  template:
    metadata:
      labels:
        app: app-1306
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
