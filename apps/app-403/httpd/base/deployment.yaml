apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-403
  labels:
    app: app-403
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-403
  template:
    metadata:
      labels:
        app: app-403
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
