apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-917
  labels:
    app: app-917
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-917
  template:
    metadata:
      labels:
        app: app-917
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
