apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-622
  labels:
    app: app-622
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-622
  template:
    metadata:
      labels:
        app: app-622
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
