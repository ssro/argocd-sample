apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-993
  labels:
    app: app-993
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-993
  template:
    metadata:
      labels:
        app: app-993
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
