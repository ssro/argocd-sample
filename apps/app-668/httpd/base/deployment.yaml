apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-668
  labels:
    app: app-668
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-668
  template:
    metadata:
      labels:
        app: app-668
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
