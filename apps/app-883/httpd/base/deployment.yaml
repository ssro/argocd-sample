apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-883
  labels:
    app: app-883
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-883
  template:
    metadata:
      labels:
        app: app-883
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
