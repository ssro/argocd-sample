apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-779
  labels:
    app: app-779
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-779
  template:
    metadata:
      labels:
        app: app-779
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
