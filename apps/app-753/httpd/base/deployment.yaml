apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-753
  labels:
    app: app-753
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-753
  template:
    metadata:
      labels:
        app: app-753
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
