apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1421
  labels:
    app: app-1421
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1421
  template:
    metadata:
      labels:
        app: app-1421
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
