apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-948
  labels:
    app: app-948
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-948
  template:
    metadata:
      labels:
        app: app-948
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
