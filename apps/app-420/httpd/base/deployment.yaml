apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-420
  labels:
    app: app-420
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-420
  template:
    metadata:
      labels:
        app: app-420
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
