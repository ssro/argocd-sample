apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-921
  labels:
    app: app-921
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-921
  template:
    metadata:
      labels:
        app: app-921
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
