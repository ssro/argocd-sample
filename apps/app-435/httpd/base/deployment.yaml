apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-435
  labels:
    app: app-435
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-435
  template:
    metadata:
      labels:
        app: app-435
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
