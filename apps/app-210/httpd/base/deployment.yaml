apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-210
  labels:
    app: app-210
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-210
  template:
    metadata:
      labels:
        app: app-210
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
