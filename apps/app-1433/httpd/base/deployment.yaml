apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1433
  labels:
    app: app-1433
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1433
  template:
    metadata:
      labels:
        app: app-1433
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
