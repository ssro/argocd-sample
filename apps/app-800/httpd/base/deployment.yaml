apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-800
  labels:
    app: app-800
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-800
  template:
    metadata:
      labels:
        app: app-800
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
