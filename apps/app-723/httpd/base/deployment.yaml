apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-723
  labels:
    app: app-723
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-723
  template:
    metadata:
      labels:
        app: app-723
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
