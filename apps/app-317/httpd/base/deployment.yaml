apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-317
  labels:
    app: app-317
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-317
  template:
    metadata:
      labels:
        app: app-317
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
