apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-250
  labels:
    app: app-250
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-250
  template:
    metadata:
      labels:
        app: app-250
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
