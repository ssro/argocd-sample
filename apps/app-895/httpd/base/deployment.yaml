apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-895
  labels:
    app: app-895
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-895
  template:
    metadata:
      labels:
        app: app-895
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
