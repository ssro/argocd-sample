apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1429
  labels:
    app: app-1429
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1429
  template:
    metadata:
      labels:
        app: app-1429
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
