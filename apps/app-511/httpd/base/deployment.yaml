apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-511
  labels:
    app: app-511
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-511
  template:
    metadata:
      labels:
        app: app-511
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
