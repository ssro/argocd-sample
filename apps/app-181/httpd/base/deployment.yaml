apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-181
  labels:
    app: app-181
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-181
  template:
    metadata:
      labels:
        app: app-181
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
