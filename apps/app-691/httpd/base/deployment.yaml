apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-691
  labels:
    app: app-691
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-691
  template:
    metadata:
      labels:
        app: app-691
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
