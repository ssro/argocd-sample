apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-609
  labels:
    app: app-609
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-609
  template:
    metadata:
      labels:
        app: app-609
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
