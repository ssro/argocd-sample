apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-644
  labels:
    app: app-644
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-644
  template:
    metadata:
      labels:
        app: app-644
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
