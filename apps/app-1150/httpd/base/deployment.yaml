apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1150
  labels:
    app: app-1150
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1150
  template:
    metadata:
      labels:
        app: app-1150
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
