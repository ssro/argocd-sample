apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-450
  labels:
    app: app-450
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-450
  template:
    metadata:
      labels:
        app: app-450
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
