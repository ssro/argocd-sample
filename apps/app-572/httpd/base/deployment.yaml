apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-572
  labels:
    app: app-572
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-572
  template:
    metadata:
      labels:
        app: app-572
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
