apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-352
  labels:
    app: app-352
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-352
  template:
    metadata:
      labels:
        app: app-352
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
