apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-215
  labels:
    app: app-215
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-215
  template:
    metadata:
      labels:
        app: app-215
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
