apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-983
  labels:
    app: app-983
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-983
  template:
    metadata:
      labels:
        app: app-983
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
