apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-708
  labels:
    app: app-708
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-708
  template:
    metadata:
      labels:
        app: app-708
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
