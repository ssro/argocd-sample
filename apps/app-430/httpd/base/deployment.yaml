apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-430
  labels:
    app: app-430
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-430
  template:
    metadata:
      labels:
        app: app-430
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
