apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-981
  labels:
    app: app-981
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-981
  template:
    metadata:
      labels:
        app: app-981
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
