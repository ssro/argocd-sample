apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-79
  labels:
    app: app-79
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-79
  template:
    metadata:
      labels:
        app: app-79
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
