apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-554
  labels:
    app: app-554
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-554
  template:
    metadata:
      labels:
        app: app-554
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
