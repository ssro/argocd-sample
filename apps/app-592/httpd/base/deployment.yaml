apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-592
  labels:
    app: app-592
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-592
  template:
    metadata:
      labels:
        app: app-592
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
