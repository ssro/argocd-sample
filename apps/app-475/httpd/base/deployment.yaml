apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-475
  labels:
    app: app-475
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-475
  template:
    metadata:
      labels:
        app: app-475
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
