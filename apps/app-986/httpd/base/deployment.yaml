apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-986
  labels:
    app: app-986
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-986
  template:
    metadata:
      labels:
        app: app-986
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
