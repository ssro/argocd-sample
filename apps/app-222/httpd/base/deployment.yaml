apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-222
  labels:
    app: app-222
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-222
  template:
    metadata:
      labels:
        app: app-222
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
