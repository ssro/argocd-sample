apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-671
  labels:
    app: app-671
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-671
  template:
    metadata:
      labels:
        app: app-671
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
