apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1374
  labels:
    app: app-1374
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1374
  template:
    metadata:
      labels:
        app: app-1374
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
