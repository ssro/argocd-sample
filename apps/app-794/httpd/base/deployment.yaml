apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-794
  labels:
    app: app-794
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-794
  template:
    metadata:
      labels:
        app: app-794
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
