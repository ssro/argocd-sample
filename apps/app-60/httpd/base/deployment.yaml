apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-60
  labels:
    app: app-60
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-60
  template:
    metadata:
      labels:
        app: app-60
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
