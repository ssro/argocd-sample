apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-924
  labels:
    app: app-924
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-924
  template:
    metadata:
      labels:
        app: app-924
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
