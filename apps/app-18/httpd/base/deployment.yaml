apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-18
  labels:
    app: app-18
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-18
  template:
    metadata:
      labels:
        app: app-18
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
