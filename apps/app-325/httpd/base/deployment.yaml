apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-325
  labels:
    app: app-325
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-325
  template:
    metadata:
      labels:
        app: app-325
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
