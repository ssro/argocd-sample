apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-929
  labels:
    app: app-929
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-929
  template:
    metadata:
      labels:
        app: app-929
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
