apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-216
  labels:
    app: app-216
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-216
  template:
    metadata:
      labels:
        app: app-216
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
