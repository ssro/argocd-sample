apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-127
  labels:
    app: app-127
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-127
  template:
    metadata:
      labels:
        app: app-127
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
