apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-616
  labels:
    app: app-616
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-616
  template:
    metadata:
      labels:
        app: app-616
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
