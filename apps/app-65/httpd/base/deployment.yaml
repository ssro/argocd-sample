apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-65
  labels:
    app: app-65
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-65
  template:
    metadata:
      labels:
        app: app-65
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
