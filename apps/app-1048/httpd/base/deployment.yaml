apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1048
  labels:
    app: app-1048
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1048
  template:
    metadata:
      labels:
        app: app-1048
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
