apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-171
  labels:
    app: app-171
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-171
  template:
    metadata:
      labels:
        app: app-171
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
