apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-726
  labels:
    app: app-726
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-726
  template:
    metadata:
      labels:
        app: app-726
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
