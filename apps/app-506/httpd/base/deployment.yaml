apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-506
  labels:
    app: app-506
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-506
  template:
    metadata:
      labels:
        app: app-506
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
