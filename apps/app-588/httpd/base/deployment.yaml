apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-588
  labels:
    app: app-588
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-588
  template:
    metadata:
      labels:
        app: app-588
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
