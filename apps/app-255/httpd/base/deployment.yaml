apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-255
  labels:
    app: app-255
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-255
  template:
    metadata:
      labels:
        app: app-255
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
