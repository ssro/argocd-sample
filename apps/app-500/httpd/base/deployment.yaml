apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-500
  labels:
    app: app-500
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-500
  template:
    metadata:
      labels:
        app: app-500
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
