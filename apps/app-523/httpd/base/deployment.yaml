apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-523
  labels:
    app: app-523
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-523
  template:
    metadata:
      labels:
        app: app-523
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
