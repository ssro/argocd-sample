apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-925
  labels:
    app: app-925
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-925
  template:
    metadata:
      labels:
        app: app-925
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
