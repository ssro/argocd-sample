apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1302
  labels:
    app: app-1302
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1302
  template:
    metadata:
      labels:
        app: app-1302
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
