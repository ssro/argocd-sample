apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-637
  labels:
    app: app-637
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-637
  template:
    metadata:
      labels:
        app: app-637
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
