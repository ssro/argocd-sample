apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-773
  labels:
    app: app-773
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-773
  template:
    metadata:
      labels:
        app: app-773
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
