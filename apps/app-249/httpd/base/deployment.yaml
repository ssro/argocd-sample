apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-249
  labels:
    app: app-249
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-249
  template:
    metadata:
      labels:
        app: app-249
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
