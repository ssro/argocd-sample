apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-8
  labels:
    app: app-8
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-8
  template:
    metadata:
      labels:
        app: app-8
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
