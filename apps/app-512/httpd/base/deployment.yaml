apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-512
  labels:
    app: app-512
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-512
  template:
    metadata:
      labels:
        app: app-512
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
