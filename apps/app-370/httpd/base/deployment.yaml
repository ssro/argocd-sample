apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-370
  labels:
    app: app-370
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-370
  template:
    metadata:
      labels:
        app: app-370
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
