apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-193
  labels:
    app: app-193
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-193
  template:
    metadata:
      labels:
        app: app-193
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
