apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-559
  labels:
    app: app-559
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-559
  template:
    metadata:
      labels:
        app: app-559
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
