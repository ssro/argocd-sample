apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-885
  labels:
    app: app-885
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-885
  template:
    metadata:
      labels:
        app: app-885
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
