apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-806
  labels:
    app: app-806
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-806
  template:
    metadata:
      labels:
        app: app-806
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
