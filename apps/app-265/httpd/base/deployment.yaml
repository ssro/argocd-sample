apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-265
  labels:
    app: app-265
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-265
  template:
    metadata:
      labels:
        app: app-265
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
