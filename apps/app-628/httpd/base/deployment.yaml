apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-628
  labels:
    app: app-628
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-628
  template:
    metadata:
      labels:
        app: app-628
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
