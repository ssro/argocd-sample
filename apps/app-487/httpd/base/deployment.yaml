apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-487
  labels:
    app: app-487
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-487
  template:
    metadata:
      labels:
        app: app-487
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
