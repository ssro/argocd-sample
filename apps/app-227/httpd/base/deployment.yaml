apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-227
  labels:
    app: app-227
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-227
  template:
    metadata:
      labels:
        app: app-227
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
