apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-825
  labels:
    app: app-825
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-825
  template:
    metadata:
      labels:
        app: app-825
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
