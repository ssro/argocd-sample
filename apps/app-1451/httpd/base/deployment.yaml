apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1451
  labels:
    app: app-1451
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1451
  template:
    metadata:
      labels:
        app: app-1451
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
