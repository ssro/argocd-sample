apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-762
  labels:
    app: app-762
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-762
  template:
    metadata:
      labels:
        app: app-762
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
