apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-50
  labels:
    app: app-50
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-50
  template:
    metadata:
      labels:
        app: app-50
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
