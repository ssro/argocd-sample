apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-539
  labels:
    app: app-539
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-539
  template:
    metadata:
      labels:
        app: app-539
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
