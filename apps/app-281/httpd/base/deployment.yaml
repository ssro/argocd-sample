apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-281
  labels:
    app: app-281
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-281
  template:
    metadata:
      labels:
        app: app-281
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
