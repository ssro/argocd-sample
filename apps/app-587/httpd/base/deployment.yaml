apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-587
  labels:
    app: app-587
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-587
  template:
    metadata:
      labels:
        app: app-587
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
