apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-99
  labels:
    app: app-99
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-99
  template:
    metadata:
      labels:
        app: app-99
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
