apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-733
  labels:
    app: app-733
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-733
  template:
    metadata:
      labels:
        app: app-733
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
