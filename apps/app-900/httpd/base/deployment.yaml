apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-900
  labels:
    app: app-900
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-900
  template:
    metadata:
      labels:
        app: app-900
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
