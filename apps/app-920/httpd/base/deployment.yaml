apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-920
  labels:
    app: app-920
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-920
  template:
    metadata:
      labels:
        app: app-920
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
