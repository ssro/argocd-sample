apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-71
  labels:
    app: app-71
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-71
  template:
    metadata:
      labels:
        app: app-71
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
