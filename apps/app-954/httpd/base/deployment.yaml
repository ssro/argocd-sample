apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-954
  labels:
    app: app-954
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-954
  template:
    metadata:
      labels:
        app: app-954
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
