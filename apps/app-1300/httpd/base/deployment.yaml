apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1300
  labels:
    app: app-1300
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1300
  template:
    metadata:
      labels:
        app: app-1300
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
