apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-64
  labels:
    app: app-64
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-64
  template:
    metadata:
      labels:
        app: app-64
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
