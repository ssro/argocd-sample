apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-384
  labels:
    app: app-384
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-384
  template:
    metadata:
      labels:
        app: app-384
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
