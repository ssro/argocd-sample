apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-996
  labels:
    app: app-996
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-996
  template:
    metadata:
      labels:
        app: app-996
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
