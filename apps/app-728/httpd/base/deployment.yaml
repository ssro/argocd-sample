apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-728
  labels:
    app: app-728
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-728
  template:
    metadata:
      labels:
        app: app-728
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
