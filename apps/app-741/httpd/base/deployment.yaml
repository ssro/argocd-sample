apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-741
  labels:
    app: app-741
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-741
  template:
    metadata:
      labels:
        app: app-741
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
