apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-813
  labels:
    app: app-813
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-813
  template:
    metadata:
      labels:
        app: app-813
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
