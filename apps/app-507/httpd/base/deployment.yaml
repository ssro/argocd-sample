apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-507
  labels:
    app: app-507
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-507
  template:
    metadata:
      labels:
        app: app-507
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
