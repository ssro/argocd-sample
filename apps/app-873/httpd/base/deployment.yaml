apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-873
  labels:
    app: app-873
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-873
  template:
    metadata:
      labels:
        app: app-873
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
