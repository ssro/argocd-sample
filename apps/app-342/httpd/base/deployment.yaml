apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-342
  labels:
    app: app-342
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-342
  template:
    metadata:
      labels:
        app: app-342
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
