apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-926
  labels:
    app: app-926
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-926
  template:
    metadata:
      labels:
        app: app-926
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
