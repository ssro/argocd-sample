apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-864
  labels:
    app: app-864
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-864
  template:
    metadata:
      labels:
        app: app-864
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
