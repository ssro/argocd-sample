apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-977
  labels:
    app: app-977
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-977
  template:
    metadata:
      labels:
        app: app-977
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
