apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-365
  labels:
    app: app-365
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-365
  template:
    metadata:
      labels:
        app: app-365
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
