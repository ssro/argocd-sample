apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-683
  labels:
    app: app-683
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-683
  template:
    metadata:
      labels:
        app: app-683
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
