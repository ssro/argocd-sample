apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-15
  labels:
    app: app-15
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-15
  template:
    metadata:
      labels:
        app: app-15
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
