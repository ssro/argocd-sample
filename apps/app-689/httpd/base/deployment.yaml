apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-689
  labels:
    app: app-689
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-689
  template:
    metadata:
      labels:
        app: app-689
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
