apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-661
  labels:
    app: app-661
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-661
  template:
    metadata:
      labels:
        app: app-661
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
