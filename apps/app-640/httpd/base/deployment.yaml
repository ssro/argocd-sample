apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-640
  labels:
    app: app-640
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-640
  template:
    metadata:
      labels:
        app: app-640
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
