apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-952
  labels:
    app: app-952
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-952
  template:
    metadata:
      labels:
        app: app-952
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
