apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-345
  labels:
    app: app-345
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-345
  template:
    metadata:
      labels:
        app: app-345
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
