apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-189
  labels:
    app: app-189
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-189
  template:
    metadata:
      labels:
        app: app-189
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
