apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-744
  labels:
    app: app-744
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-744
  template:
    metadata:
      labels:
        app: app-744
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
