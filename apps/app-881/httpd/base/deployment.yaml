apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-881
  labels:
    app: app-881
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-881
  template:
    metadata:
      labels:
        app: app-881
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
