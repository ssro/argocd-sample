apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-922
  labels:
    app: app-922
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-922
  template:
    metadata:
      labels:
        app: app-922
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
