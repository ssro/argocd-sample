apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-482
  labels:
    app: app-482
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-482
  template:
    metadata:
      labels:
        app: app-482
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
