apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-604
  labels:
    app: app-604
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-604
  template:
    metadata:
      labels:
        app: app-604
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
