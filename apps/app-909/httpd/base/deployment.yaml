apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-909
  labels:
    app: app-909
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-909
  template:
    metadata:
      labels:
        app: app-909
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
