apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-4
  labels:
    app: app-4
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-4
  template:
    metadata:
      labels:
        app: app-4
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
