apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-544
  labels:
    app: app-544
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-544
  template:
    metadata:
      labels:
        app: app-544
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
