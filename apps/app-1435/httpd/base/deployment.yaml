apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1435
  labels:
    app: app-1435
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1435
  template:
    metadata:
      labels:
        app: app-1435
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
