apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-560
  labels:
    app: app-560
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-560
  template:
    metadata:
      labels:
        app: app-560
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
