apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-600
  labels:
    app: app-600
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-600
  template:
    metadata:
      labels:
        app: app-600
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
