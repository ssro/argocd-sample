apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-555
  labels:
    app: app-555
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-555
  template:
    metadata:
      labels:
        app: app-555
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
