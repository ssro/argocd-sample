apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1100
  labels:
    app: app-1100
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1100
  template:
    metadata:
      labels:
        app: app-1100
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
