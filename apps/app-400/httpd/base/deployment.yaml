apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-400
  labels:
    app: app-400
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-400
  template:
    metadata:
      labels:
        app: app-400
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
