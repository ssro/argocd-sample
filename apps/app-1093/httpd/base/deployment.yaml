apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1093
  labels:
    app: app-1093
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1093
  template:
    metadata:
      labels:
        app: app-1093
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
