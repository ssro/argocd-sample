apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-962
  labels:
    app: app-962
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-962
  template:
    metadata:
      labels:
        app: app-962
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
