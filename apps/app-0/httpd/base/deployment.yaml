apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-0
  labels:
    app: app-0
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-0
  template:
    metadata:
      labels:
        app: app-0
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
