apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-649
  labels:
    app: app-649
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-649
  template:
    metadata:
      labels:
        app: app-649
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
