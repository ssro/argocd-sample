apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-670
  labels:
    app: app-670
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-670
  template:
    metadata:
      labels:
        app: app-670
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
