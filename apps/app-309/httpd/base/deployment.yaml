apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-309
  labels:
    app: app-309
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-309
  template:
    metadata:
      labels:
        app: app-309
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
