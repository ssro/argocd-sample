apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1080
  labels:
    app: app-1080
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1080
  template:
    metadata:
      labels:
        app: app-1080
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
