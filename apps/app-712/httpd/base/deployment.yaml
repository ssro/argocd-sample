apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-712
  labels:
    app: app-712
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-712
  template:
    metadata:
      labels:
        app: app-712
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
