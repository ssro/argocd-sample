apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-288
  labels:
    app: app-288
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-288
  template:
    metadata:
      labels:
        app: app-288
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
