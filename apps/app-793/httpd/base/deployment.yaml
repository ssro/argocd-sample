apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-793
  labels:
    app: app-793
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-793
  template:
    metadata:
      labels:
        app: app-793
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
