apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-1000
  labels:
    app: app-1000
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1000
  template:
    metadata:
      labels:
        app: app-1000
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
