apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-149
  labels:
    app: app-149
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-149
  template:
    metadata:
      labels:
        app: app-149
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
