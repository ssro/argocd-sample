apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-354
  labels:
    app: app-354
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-354
  template:
    metadata:
      labels:
        app: app-354
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
