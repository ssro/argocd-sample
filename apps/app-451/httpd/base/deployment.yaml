apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-451
  labels:
    app: app-451
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-451
  template:
    metadata:
      labels:
        app: app-451
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
