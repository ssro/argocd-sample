apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-700
  labels:
    app: app-700
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-700
  template:
    metadata:
      labels:
        app: app-700
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
