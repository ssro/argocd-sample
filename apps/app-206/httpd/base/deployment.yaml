apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-206
  labels:
    app: app-206
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-206
  template:
    metadata:
      labels:
        app: app-206
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
