apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-703
  labels:
    app: app-703
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-703
  template:
    metadata:
      labels:
        app: app-703
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
