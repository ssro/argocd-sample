apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-706
  labels:
    app: app-706
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-706
  template:
    metadata:
      labels:
        app: app-706
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
