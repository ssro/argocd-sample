apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-262
  labels:
    app: app-262
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-262
  template:
    metadata:
      labels:
        app: app-262
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
