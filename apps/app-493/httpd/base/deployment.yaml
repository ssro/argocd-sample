apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-493
  labels:
    app: app-493
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-493
  template:
    metadata:
      labels:
        app: app-493
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
