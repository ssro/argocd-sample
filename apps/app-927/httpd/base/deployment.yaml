apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-927
  labels:
    app: app-927
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-927
  template:
    metadata:
      labels:
        app: app-927
    spec:
      containers:
      - name: web
        image: httpd
        ports:
        - containerPort: 80
